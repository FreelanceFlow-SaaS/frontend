export const ACCESS_TOKEN_STORAGE_KEY = "ff_access_token";
export const ACCESS_TOKEN_COOKIE_NAME = "ff_access_token";

/** Cookie max-age: 7 days (aligns with refresh token lifetime on the API). */
export const ACCESS_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 7;
